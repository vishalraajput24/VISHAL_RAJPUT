kiteconnect
# kiteconnect 5.2.0 metadata pins autobahn[twisted]==19.11.2, but the latest
# autobahn is API-compatible with its ticker (verified: TLS + WS handshake against
# ws.kite.trade succeeds, reactor drives cleanly). Pin latest so reinstalls don't
# downgrade the websocket stack. Revisit if kiteconnect changes its autobahn usage.
autobahn[twisted]>=25.12.2
pyotp
PyYAML
pandas
numpy
requests
pytz
